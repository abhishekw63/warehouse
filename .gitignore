.venv
output_grn
output
__pycache__
*.pyc
build
dist
*.egg-info
*.spec
output_parsed
po_output
staticfiles
*Calculation Data
*output_online
db.sqlite3
*.log
online_po_management\Calculation Data\.update_history.json
online_po_management\output_online\*.json
calculation*

# Claude Code — local config/session data (not shared)
.claude/

# Standalone (GT Mass) — runtime state, local config, data & outputs
mt_select_seq.json
standalone_files/mt_select_seq.json
standalone_files/mt_select_config.json
standalone_files/data_mt/
standalone_files/Output_HG/

# Auto mode — history DB lives under Dump/Tracker (local/OneDrive, not source)
Dump/
*.db

# Generated channel outputs (MT Select INST / SS / etc.)
Output_*/

# Django web app media (uploaded POs + generated workbooks — runtime, not source)
media/

# Python tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
# Dev observability perf/audit logs
logs/
