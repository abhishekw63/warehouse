# renee_cosmetics — Django B2B PO web app (online_b2b)
# Python 3.13.  Install:  py -3.13 -m venv .venv ; .venv\Scripts\pip install -r requirements.txt
# See HOSTING.md for the full LAN-hosting walkthrough.

# ── Web framework + serving ──
Django==6.0.3
waitress==3.0.2          # Windows-native WSGI server (LAN hosting; unused on PythonAnywhere)
whitenoise==6.12.0       # serve collected static files from the WSGI app
python-dotenv==1.1.0     # load a project-root .env (secrets/config off the code)
gunicorn==23.0.0         # Linux WSGI server (Render / Railway / VPS / Docker)
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2025.3

# ── Database (MySQL renee_orders, via the pymysql shim) ──
PyMySQL==1.2.0
cryptography==48.0.0     # required for MySQL caching_sha2_password auth
cffi==2.0.0
pycparser==3.0

# ── Engine (online_po_processor) data stack ──
pandas==3.0.1
numpy==2.4.3
openpyxl==3.1.5
et_xmlfile==2.0.0
xlrd==2.0.2
pyxlsb==1.0.10
python-dateutil==2.9.0.post0
six==1.17.0

# ── PDF parsing (Firstcry / Flipkart) ──
pdfplumber==0.11.9
pdfminer.six==20251230
pypdfium2==5.9.0
pillow==12.2.0

typing_extensions==4.15.0   # transitive dep (kept pinned)

# ── Dev / tooling (optional on the host) ──
ruff==0.15.18
mypy==2.1.0
pytest==9.1.1
pytest-django==4.12.0
django-stubs==6.0.5
django-stubs-ext==6.0.5
